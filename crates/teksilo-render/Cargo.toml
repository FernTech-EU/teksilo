# SPDX-License-Identifier: MPL-2.0
# SPDX-FileCopyrightText: 2026 FernTech

[package]
name = "teksilo-render"
edition = "2024"
version.workspace = true
license.workspace = true
publish.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
description = "wgpu renderer for Teksilo — rect, SDF and path pipelines with a glyph atlas."
documentation = "https://docs.rs/teksilo-render"
keywords = ["wgpu", "renderer", "graphics", "gpu", "glyph-atlas"]
categories = ["rendering::graphics-api", "graphics", "gui"]

[dependencies]
teksilo-canvas = { workspace = true }
wgpu = { workspace = true }
tiny-skia = { workspace = true }
bytemuck = { workspace = true }
thiserror = { workspace = true }
# Opening the shared offscreen device is blocking work behind an async
# wgpu API; `pollster` is the minimal executor that bridges the two so the
# `OnceLock` initialiser can stay synchronous. No transitive deps.
pollster = { workspace = true }

[dev-dependencies]
teksilo-tokens = { workspace = true }

[package.metadata.docs.rs]
all-features = true

# SPDX-License-Identifier: MPL-2.0
# SPDX-FileCopyrightText: 2026 FernTech

[package]
name = "web-view-demo"
edition = "2024"
version.workspace = true
license-file.workspace = true
authors.workspace = true
publish = false

[[bin]]
name = "web-view-demo"
path = "src/main.rs"

# The demo depends on `bastyde/web-view`, which is wry by default — so a plain
# `cargo run -p web-view-demo` renders via wry (macOS / Windows / Linux-X11).
# Add the Wayland fallback with:
#   cargo run -p web-view-demo --features servo   # wry + Servo; Servo on Wayland
[features]
default = []
# `servo` is additive — `bastyde/web-view-servo` implies `web-view` (wry), so
# this ships both engines and the runtime picks Servo only under Wayland.
servo = ["bastyde/web-view-servo"]

[dependencies]
bastyde = { workspace = true, features = [
    "automation","web-view"] }

[package]
name = "menus-and-dropdowns"
edition = "2024"
version.workspace = true
license-file.workspace = true
authors.workspace = true
publish = false

[dependencies]
# `rich-text` enables the `TextInput`-backed `.searchable()` combo box
# showcased in Section 1.
bastyde = { workspace = true, features = ["rich-text"] }

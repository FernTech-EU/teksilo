# SPDX-License-Identifier: MPL-2.0
# SPDX-FileCopyrightText: 2026 FernTech

[package]
name = "menus-and-dropdowns"
edition = "2024"
version.workspace = true
authors.workspace = true
publish = false

[dependencies]
# The `TextInput`-backed `.searchable()` combo box showcased in
# Section 1 comes in unconditionally with the default widget set.
teksilo = { workspace = true, features = ["automation"] }
# Needed for the tri-state checkable menu item (CheckState).
teksilo-data = { workspace = true }
